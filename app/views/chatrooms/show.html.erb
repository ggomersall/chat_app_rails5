<p>
  <strong>Name:</strong>
  <%= @chatroom.name %>
</p>

<div data-behaviour='messages'>
  <% 1.upto(100) do %>
    <div class=""><strong>ggomersall:</strong> Hello</div>
  <% end %>
</div>

<%= form_for [@chatroom, Message.new] do |f| %>
  <%= f.text_area :body, rows: 1, class: "form-control", autofocus: true %>
<% end %>
